language: elixir

cache:
  directories:
    - ~/.hex
    - ~/.mix
    - deps

addons:
  postgresql: '10'

services:
  - postgresql

elixir:
  - 1.6.6

otp_release:
  - 20.3

env: 
  global:
    - MIX_ENV=test
    - DATABASE_URL=ecto://postgres:@localhost/invault_test

before_script:
  - psql -c 'create database invault_test;' -U postgres
  - mix do ecto.migrate

script:
  - mix format --check-formatted
  - mix credo
  - mix test
  - mix coveralls.travis